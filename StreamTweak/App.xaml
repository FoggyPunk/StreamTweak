<Application x:Class="StreamTweak.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar">
    <Application.Resources>
        <tb:TaskbarIcon x:Key="MyNotifyIcon"
                        ToolTipText="StreamTweak"
                        TrayMouseDoubleClick="TaskbarIcon_TrayMouseDoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Settings..." Click="MenuSettings_Click" FontWeight="Bold"/>
                    <MenuItem Header="Start Streaming Mode" Click="MenuStreamingMode_Click"/>
                    <Separator/>
                    <MenuItem Header="Resolution: —" IsEnabled="False"/>
                    <MenuItem Header="Refresh Rate: —" IsEnabled="False"/>
                    <Separator/>
                    <MenuItem Header="Exit" Click="MenuExit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>
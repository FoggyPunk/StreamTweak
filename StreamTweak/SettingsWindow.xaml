<Window x:Class="StreamTweak.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StreamTweak 2.5.0 Settings" Height="460" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource TextForeground}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackground" Color="#FFF3F3F3"/>
        <SolidColorBrush x:Key="PanelBackground" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextForeground" Color="#FF202020"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FFD1D1D1"/>
        <SolidColorBrush x:Key="AccentColor" Color="{x:Static SystemParameters.WindowGlassColor}"/>
        <SolidColorBrush x:Key="AccentHoverColor" Color="#FF106EBE"/>

        <!-- Toggle Switch Style -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Track" Grid.Column="0" Width="40" Height="22"
                                    CornerRadius="11" Background="{DynamicResource BorderColor}">
                                <Ellipse x:Name="Thumb" Width="16" Height="16" Fill="White"
                                         HorizontalAlignment="Left" Margin="3,0,0,0"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" Margin="8,0,0,0"
                                              VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="21,0,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6"/>
                            <Grid Margin="8,0">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"/>
                                <Path HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0"
                                      Data="M 0 0 L 4 4 L 8 0" Stroke="{TemplateBinding Foreground}"
                                      StrokeThickness="2" StrokeEndLineCap="Round"/>
                            </Grid>
                            <ToggleButton Background="Transparent" BorderThickness="0" Focusable="False"
                                          FocusVisualStyle="{x:Null}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border Background="{DynamicResource PanelBackground}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1" CornerRadius="6" Margin="0,4,0,0">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Styles -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A000000"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentHoverColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="*"/>
         <RowDefinition Height="Auto"/>
         </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Hardware Configuration" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
            <Button x:Name="ThemeToggleButton" Content="🌙 Dark Mode" HorizontalAlignment="Right"
                    Style="{StaticResource ModernButton}" Width="110" Height="28" Padding="5,2"
                    Click="ThemeToggleButton_Click"/>
        </Grid>

        <!-- Adapter -->
        <TextBlock Text="Select Physical Network Adapter:" FontWeight="Medium" Margin="0,0,0,5" Grid.Row="1"/>
        <ComboBox x:Name="AdapterComboBox" Margin="0,0,0,10" Grid.Row="2"
                  SelectionChanged="AdapterComboBox_SelectionChanged"/>

        <!-- Current Speed -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Current Speed:" FontWeight="Medium" Margin="0,0,10,0"/>
            <TextBlock x:Name="CurrentSpeedTextBlock" Text="Detecting..." FontWeight="Bold"
                       Foreground="{DynamicResource AccentColor}"/>
        </StackPanel>

        <!-- Target Speed -->
        <TextBlock Text="Target Link Speed:" FontWeight="Medium" Margin="0,0,0,5" Grid.Row="4"/>
        <ComboBox x:Name="SpeedComboBox" Margin="0,0,0,10" Grid.Row="5"/>

        <!-- Separator -->
        <Separator Grid.Row="6" Margin="0,5,0,10" Background="{DynamicResource BorderColor}"/>

        <!-- Auto Streaming Mode Toggle -->
        <Grid Grid.Row="7" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Auto Streaming Mode" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
            <CheckBox Grid.Column="1" x:Name="AutoStreamingToggle" Style="{StaticResource ToggleSwitchStyle}" 
                      Margin="0,0,0,0" VerticalAlignment="Center" Checked="AutoStreamingToggle_Changed" 
                      Unchecked="AutoStreamingToggle_Changed"/>
        </Grid>

        <StackPanel Grid.Row="8" Orientation="Vertical" Margin="0,12,0,0">
            <TextBlock FontSize="9" Foreground="#FF808080" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,2" LineHeight="14">
                <Run Text="Auto-sets network speed to 1Gbps when Moonlight streams."/>
            </TextBlock>
            <TextBlock FontSize="9" Foreground="#FF808080" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,2" LineHeight="14">
                <Run Text="⚠️ Disconnect may occur if speed adjustment is needed. Reconnect manually if required."/>
            </TextBlock>
            <TextBlock FontSize="9" Foreground="#FF808080" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,0" LineHeight="14">
                <Run Text="Requires Sunshine/Apollo in default installation folders."/>
            </TextBlock>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="9" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="StreamingModeButton" Width="170" Height="32"
                    Click="StreamingModeButton_Click" Style="{StaticResource ModernButton}"
                    HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="pack://application:,,,/Resources/streammodeok.ico" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock Text="Start Streaming Mode" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Image Grid.Column="1" Source="pack://application:,,,/Resources/streamtweak.ico" Width="24" Height="24" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="Apply Settings" Width="110" Height="32" Click="ApplyButton_Click"
                    Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right"/>
        </Grid>

        <!-- Info Text -->
        <StackPanel Grid.Row="10" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,6,0,0" VerticalAlignment="Center">
            <TextBlock Text="Streaming Mode auto-sets the Target Link Speed to 1Gbps. Stop to revert to original Speed." 
                       FontSize="9" Foreground="#FF808080" TextAlignment="Center" Margin="0,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>